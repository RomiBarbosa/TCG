using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BattleManager : MonoBehaviour
{
    List<Unit> player0 = new List<Unit>();
    List<Unit> player1 = new List<Unit>();

    public void AddParticipants(List<Card> attacker, List<Card> defender)
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
